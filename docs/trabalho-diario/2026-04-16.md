# Diario — 2026-04-16

---

### [2026-04-16 08:16] Diario de trabalho — convencao e integracao ao CLAUDE.md

| Campo | Valor |
|-------|--------|
| **Projeto** | G360 |
| **Work item** | (processo interno) |

**Descricao**
- Criada pasta `docs/trabalho-diario/` com `README.md` (finalidade, nomenclatura por dia, template, exemplo, quando registrar).
- `CLAUDE.md`: nova secao **Diario de trabalho** com regras obrigatorias ao concluir entregas materiais; diario nao substitui `docs/CHANGELOG.md`.
- `docs/CHANGELOG.md`: entrada **Docs** em 2026-04-16 listando os arquivos acima.

---

### [2026-04-16 08:25] Diario retroativo — trabalho de 2026-04-15

| Campo | Valor |
|-------|--------|
| **Projeto** | G360 |
| **Work item** | — |

**Descricao**
- Criado `docs/trabalho-diario/2026-04-15.md` com sete entradas agrupadas (form/modais/TaskModal, reference users, BUG-005, US-021, US-018/US-019, US-020, E2E Playwright), baseadas na secao `[2026-04-15]` do changelog; nota de horario n/d (retroativo).
- `docs/CHANGELOG.md`: bullet **Docs** em 2026-04-16 referenciando o arquivo retroativo.

---

### [2026-04-16 08:28] Ambiente local — backend, frontend e PostgreSQL

| Campo | Valor |
|-------|--------|
| **Projeto** | G360 |
| **Work item** | — |

**Descricao**
- `npm run dev` no BACKEND (porta **8500**) e no FRONTEND (Vite **5173**, `--host`).
- PostgreSQL local iniciado via `scripts/start-local-postgres.ps1` (porta **5433**); health `/api/v1/health` passou a `healthy`.
- Ajuste em `BACKEND/src/middlewares/rate-limit.middleware.js`: `globalRateLimitKey` usa `ipKeyGenerator` do `express-rate-limit` (elimina erro de validacao IPv6 no startup).

---

### [2026-04-16 08:42] Seed de 10 fornecedores (todos os tenants)

| Campo | Valor |
|-------|--------|
| **Projeto** | G360 |
| **Work item** | — |

**Descricao**
- Criados `BACKEND/src/scripts/seed-suppliers.js` (10 fornecedores, CNPJ valido, `upsert` por documento) e `seed-suppliers-all-tenants.js` (padrao `seed-catalog-all-tenants`).
- `package.json`: script `seed:suppliers:all`; execucao OK em `master` (public) e `liotecnica` (tenant_liotecnica), 10 registros cada.
- `docs/CHANGELOG.md` atualizado (Added).

---

### [2026-04-16 09:15] Seed 3 áreas + E2E isolamento entre CCs

| Campo | Valor |
|-------|--------|
| **Projeto** | G360 |
| **Work item** | — |

**Descricao**
- `BACKEND/src/scripts/seed-three-areas-workflow.js` + `seed-three-areas-all-tenants.js`: 3 departamentos/CCs, gestores (Manager) e colaboradores (Collaborator), projeto aprovado, ProjectTask, Task, Incident, CorporateRisk (owner colaborador), Asset, ChangeRequest com ativo, Expense; `npm run seed:three-areas:all`.
- `FRONTEND/e2e/three-areas-isolation.spec.ts` + `auth.helper.ts` (`loginWithCredentialsAndDashboard`, `THREE_AREAS_SEED`); testes de projetos (gestor) e incidentes/riscos (colaborador TI — gestor tem VIEW_ALL_CC em incidentes).
- `RisksPage.jsx`: `data-testid="risks-view-list"` para E2E alternar à lista.
- `docs/CHANGELOG.md` atualizado.
